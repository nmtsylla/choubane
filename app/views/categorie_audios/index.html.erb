<%- model_class = CategorieAudio -%>
<div class="page-header">
  <h1><%=t '.title', :default => CategorieAudio %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th><%= model_class.human_attribute_name(:name) %></th>
      <th><%= model_class.human_attribute_name(:description) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @categorie_audios.each do |categorie_audio| %>
      <tr>
        <td><%= categorie_audio.id %></td>
        <td><%= categorie_audio.name %></td>
        <td><%= categorie_audio.description %></td>
        <td><%=l categorie_audio.created_at, format: :long %></td>
        <td>
          <%= link_to categorie_audio_path(categorie_audio), :class => 'btn btn-xs', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
            <%= glyph 'info-sign' %>
            <%= glyph 'pencil' %>
          <%- end -%>
          <%= link_to categorie_audio_path(categorie_audio), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs', :title => "#{ t('.destroy', :default => t('helpers.links.destroy')) }" do %>
            <%= glyph 'remove' %>
          <%- end -%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_categorie_audio_path,
            :class => 'btn btn-primary' %>
